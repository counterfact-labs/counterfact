# Development convenience file.
# The authoritative dependency spec is pyproject.toml.
#
# For development:    pip install -e ".[all,dev]"
# For production:     pip install counterfact
# For specific extras: pip install counterfact[google,cli]

# Core dependencies
langgraph>=0.2.0
numpy>=1.20.0

# LLM providers (optional — install via pip install counterfact[all])
anthropic>=0.40.0
google-genai>=1.0.0

# CLI (optional — install via pip install counterfact[cli])
rich>=13.0.0

# OpenAI Agents SDK adapter (optional — install via pip install counterfact[openai-agents])
openai-agents

# Braintrust adapter (optional — install via pip install counterfact[braintrust])
braintrust
autoevals

# Development tools (install via pip install counterfact[dev])
pytest>=8.0.0
pytest-cov>=5.0.0
pytest-asyncio>=0.23.0
ruff>=0.4.0
mypy>=1.10.0
